# Server host/port
HOST=127.0.0.1
PORT=5000

# Choose one or set both. If both are set, OpenAI is preferred unless a Claude key is sent from UI.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional model overrides
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-sonnet-4-5
